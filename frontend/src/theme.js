import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1e3a8a', // Dark blue
    },
    secondary: {
      main: '#6d28d9', // Purple or any color you prefer
    },
  },
});

export default theme;
